services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: vsms
      POSTGRES_USER: vsms
      POSTGRES_PASSWORD: vsms
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
